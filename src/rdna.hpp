#include <random>
#include <iostream>
#include <string>
#include <main.cpp>
using namespace std;
int main ()
{	
	mt19937 eng(seed);
	uniform_string_distribution<> sequence(bases);
	for (i = 0; i < n; i++)
		randDNA = sequence(eng)
	
	
	
